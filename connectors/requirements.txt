# Pipeline / connector runtime deps (separate from the research training deps).
# The SPI core (pivot, registry, alignment, conformance) needs none of these;
# they are required only to actually run Beam pipelines and write Parquet.
apache-beam[gcp]>=2.55
pyarrow>=14.0
# s3fs>=2024.2    # enable for MinIO/S3 (s3://) Parquet output